Now make Pretext genuinely necessary in handwriter.

Current goal:
Use Pretext to create layout behavior that is visibly more interesting than ordinary CSS text flow.

Requirements:
- Seed the scrapbook page with a long background body of text.
- The movable scrapbook shapes should act as obstacles.
- The background text should reflow around those obstacles using Pretext.
- The value of Pretext should be obvious at a glance.
- Prioritize correct layout logic over visual polish.
- Sticky notes can use rectangular obstacle bounds.
- Circles should use per-row geometry if feasible.
- Hearts can use a simplified approximation for now if needed.
- Keep the OCR architecture unchanged.

Implementation guidance:
- Shared coordinate space matters.
- If the obstacle model is too naive, fix the geometry before polishing visuals.
- The result should clearly show text routing around shapes rather than sitting underneath them.

Important:
- Commit and push this as its own milestone.

After implementing:
- explain exactly why Pretext is justified in this version
- tell me what failure modes I should test for
- tell me what parts of the obstacle model are still approximate

---

the color scheme and style is a little hard for visibility..lets take some inspiration and copy the theme/color/layout from Documents/agent-experiments/handwriter. We do not need to add in pretext just yet