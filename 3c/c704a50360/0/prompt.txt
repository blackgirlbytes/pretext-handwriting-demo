same with the image upload..when we upload it..it should just start the recognition process

---

Add Camera mode and clean up the input UX.

Requirements:
- Keep Draw mode working with Google Input Tools over HTTP.
- Keep Upload Image working through the OpenAI Responses API.
- Add a Camera mode that captures a frame and sends it through the same server-side OpenAI image-recognition flow as Upload Image.
- The app should now clearly support three modes:
  - Draw
  - Upload Image
  - Camera
- Each mode should show only the controls relevant to that mode.
- Prefer the rear camera when possible so handwriting is less likely to be mirrored like a selfie preview.
- Reuse the same image recognition endpoint for Upload Image and Camera.

Implementation preferences:
- Keep the UI easy to scan.
- Avoid mixing camera controls into other modes.
- Keep the OCR architecture explicit and unchanged.

Important:
- This milestone is about Camera mode and input UX cleanup.
- Commit and push this as its own milestone.

After implementing:
- summarize the UX changes
- explicitly confirm the final OCR architecture
- tell me any browser/device limitations I should know about
- tell me what I should test manually